return {
  "serhez/bento.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  opts = {
    ui = {
      mode = "floating",
      floating = {
        minimal_menu = "full",
      },
    },
    labels = {
      enable = true,
    },
  },
}
